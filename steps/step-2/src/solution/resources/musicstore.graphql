type Genre {
  id: ID
  name: String!
  image: String
}

type Album {
  id: ID
  name: String!
  genre: Genre!
  artist: String!
  image: String
}

type Query {
  genres: [Genre!]
  albums(genre: ID): [Album!]
}

schema {
  query: Query
}
